package com.keboom.Appserver;

import org.junit.jupiter.api.Test;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

/**
 * {@code @author:} keboom
 * {@code @date:} 2023/10/13
 * {@code @description:}
 */
public class MobiControllerTest {

    @Test
    public void test() {
        int tryCount = 1;
        System.out.println(Math.pow(10, tryCount++));
        System.out.println(Math.pow(10, tryCount++));
        System.out.println(Math.pow(10, tryCount++));



    }
}
